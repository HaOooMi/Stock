# 机器学习量化选股框架

基于 Learning-to-Rank 的 A 股多因子选股系统，实现从数据加载、因子工程、模型训练到组合回测的完整量化研究流程。

---

## 📌 项目特色

| 特性 | 说明 |
|------|------|
| **完整研究流程** | 数据快照 → 因子工程 → 模型训练 → 横截面评估 → 组合回测 |
| **工业级数据管理** | 版本化快照、PIT 对齐、7 层可交易性过滤 |
| **三条线对比** | Regression / Reg-on-Rank / LambdaRank 对比实验 |
| **漂移监控** | PSI 特征漂移 + IC/Spread 预测漂移双重检测 |
| **无偏回测** | Open-to-Open 执行假设，符合 A 股 T+1 制度 |

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据加载层                                │
│  InfluxDB → MarketDataLoader → TradabilityFilter → PITAligner  │
│                    → DataSnapshot (版本化存储)                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        因子工程层                                │
│  FeatureEngineering + FactorFactory (5 大类 40+ 因子)          │
│       → Winsorize → Standardize → Neutralize (市值+行业)       │
│       → IC/ICIR 质量检查 → FactorLibraryManager (入库)         │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        模型训练层                                │
│  TimeSeriesCV (Purge+Embargo) → DriftDetector (PSI)            │
│       → Ridge / RF / LightGBM / LGBMRanker                     │
│       → DriftDetector (IC/Spread 预测漂移)                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        评估回测层                                │
│  Bucketing → CrossSectionMetrics → SimpleBacktest              │
│       → Tearsheet (HTML 报告 + 可视化图表)                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📂 目录结构详解

### data/ - 数据处理层

| 模块 | 功能 |
|------|------|
| `data_loader.py` | 统一数据接口，集成快照管理、交易过滤、PIT 对齐 |
| `data_snapshot.py` | 版本化数据快照（格式：`ds_2025Q4_v1`），含完整性校验 |
| `market_data_loader.py` | InfluxDB 行情加载 + MySQL 元数据加载 |
| `time_series_cv.py` | Purge + Embargo 时序切分，支持 Walk-Forward |
| `tradability_filter.py` | 7 层过滤：ST/停牌/涨跌停/上市龄/成交量/价格/换手率 |
| `pit_aligner.py` | 财务数据 PIT 对齐：公告日 + 滞后生效 + 前复权 |
| `financial_data_loader.py` | 季度财务报表加载 |

### features/ - 因子工程层

| 模块 | 功能 |
|------|------|
| `feature_engineering.py` | 基础特征工程：技术指标生成 + 特征选择 + 标准化 |
| `factor_factory.py` | 高级因子工厂：5 大因子族（动量/波动率/量价/风格/质量），含文献引用 |
| `factor_library_manager.py` | 因子库版本控制 + 入库标准 + 因子清单维护 |

**因子族覆盖**：

| 因子族 | 示例因子 | 数量 |
|--------|----------|------|
| 动量 | ROC_5/10/20/60/120、相对强弱、动量反转 | 10+ |
| 波动率 | ATR、波动率偏度、上下行波动率比 | 8+ |
| 量价 | 换手率、成交量变化、量价背离 | 8+ |
| 风格 | 市值对数、换手率排名、波动率排名 | 6+ |
| 质量 | ROE 稳定性、毛利率变化、财务健康度 | 8+ |

### targets/ - 标签处理层

| 模块 | 功能 |
|------|------|
| `target_engineering.py` | 生成未来收益率、分类标签、防泄漏处理 |
| `ranking_labels.py` | LambdaRank 分桶标签生成 |
| `label_transformer.py` | 收益率变换：GaussRank、残差化、排名化 |

### models/ - 模型层

| 模块 | 功能 |
|------|------|
| `base_model.py` | 统一接口：fit / predict / save / load |
| `lgbm_model.py` | LightGBM 回归：梯度提升树 + 早停 + 特征重要性 |
| `lgbm_ranker.py` | LightGBM LambdaRank：直接优化 NDCG 排序指标 |
| `ridge_model.py` | Ridge 回归：L2 正则化线性模型 |
| `rf_model.py` | 随机森林：Bagging 集成树 |

### evaluation/ - 评估层

| 模块 | 功能 |
|------|------|
| `cross_section_analyzer.py` | 横截面分析器：封装 IC/ICIR/Spread/单调性计算 |
| `cross_section_metrics.py` | 核心度量：Forward Returns、Rank IC、分桶收益 |
| `factor_preprocessing.py` | 因子预处理：Winsorize + Z-score + 市值行业中性化 |
| `drift_detector.py` | 漂移检测：PSI 分布漂移 + IC/Spread 预测漂移 |
| `bucketing.py` | 按日横截面分桶 + 分桶表现分析 |
| `tearsheet.py` | HTML Tearsheet 报告生成 |
| `visualization.py` | 可视化图表：净值曲线、IC 时序、分桶收益 |

### backtest/ - 回测层

| 模块 | 功能 |
|------|------|
| `simple_backtest.py` | Top-N 策略回测：等权组合 + 交易成本 + 绩效统计 |
| `top_bucket_backtest.py` | 分桶策略回测：选择预测最高桶构建组合 |
| `cluster_strategy_backtest.py` | 聚类信号策略：基于 PCA 降维 + K-means 聚类 |

### pipelines/ - 执行管道

| 模块 | 功能 |
|------|------|
| `run_baseline_pipeline.py` | 主训练管道：三条线对比（回归/Reg-on-Rank/LambdaRank） |
| `prepare_factors.py` | 因子准备：生成 → 质量检查 → IC 评估 → 入库 |
| `prepare_data_with_snapshot.py` | 数据准备 + 快照创建 |
| `main.py` | 统一入口：串联所有管道 |

---

## 🔧 核心机制详解

### 1. 数据快照系统

**版本命名规则**：`ds_YYYYQQ_vN`（如 `ds_2025Q4_v1`）

**数据质量检查项**：

| 检查项 | 红灯阈值 | 说明 |
|--------|----------|------|
| 缺失率 | > 20% | 特征缺失过多 |
| 重复数据 | > 1% | 数据重复记录 |
| 可交易样本 | < 70% | 有效样本太少 |
| 极值比例 | > 5% | 异常值过多 |

**快照包含**：
- 特征矩阵（Parquet）
- 标签数据（Parquet）
- 元数据 JSON（配置、统计、哈希校验）

### 2. 时序交叉验证（Purge + Embargo）

```
时间轴：
├────────────────┼─────────────────┼──────────────────┤
│     Train      │      Valid      │       Test       │
├────────────────┼─────────────────┼──────────────────┤
     ↑ purge_end                       ↑ embargo_start
   (train_end - purge_days)      (valid_end + embargo_days)
```

**参数设置建议**：

| 参数 | 建议值 | 说明 |
|------|--------|------|
| purge_days | ≥ max_horizon | 避免目标标签重叠 |
| embargo_days | horizon / 2 | 避免信息渗透 |
| train_ratio | 60% | 训练集占比 |
| valid_ratio | 20% | 验证集占比 |

### 3. 因子中性化（回归残差法）

对每个截面日期执行：

```
factor_neutralized = factor - β₁ × log(market_cap) - β₂ × industry_dummies
```

**处理流水线顺序**：
1. **Winsorize**：1%-99% 分位数截断
2. **Standardize**：Z-score = (x - μ) / σ
3. **Neutralize**：市值 + 行业回归残差

### 4. PSI 漂移检测

**Population Stability Index**：

```
PSI = Σ (actual_pct - expected_pct) × ln(actual_pct / expected_pct)
```

| PSI 值 | 判断 |
|--------|------|
| < 0.1 | 分布稳定 ✓ |
| 0.1 ~ 0.2 | 轻微漂移 ⚠️ |
| ≥ 0.2 | 显著漂移 ❌ |

### 5. 横截面评估指标

| 指标 | 公式 | 说明 |
|------|------|------|
| Rank IC | Spearman(预测分数, 实际收益) | 每日横截面相关 |
| ICIR | mean(IC) / std(IC) | IC 稳定性 |
| ICIR 年化 | ICIR × √252 | 年化信息比率 |
| Top-Mean Spread | mean(Top桶收益) - mean(全体收益) | 头部超额收益 |
| 单调性 | 分桶收益是否单调递增 | 因子有效性验证 |

### 6. 回测执行模式

**Open-to-Open（推荐，无偏差）**：

```
T 日收盘 → 计算信号 → T+1 日开盘买入 → T+N 日开盘卖出
```

- ✅ 符合 A 股 T+1 制度
- ✅ 信号与执行有充足时间差
- ✅ 避免收盘竞价博弈

**Close-to-Close（仅理论测试）**：

```
T 日收盘 → 计算信号 → T 日收盘买入（有前视偏差）
```

---

## 🚀 快速开始

### 1. 环境配置

```bash
# Conda 安装（推荐）
conda env create -f environment.yml
conda activate ML

# pip 安装
pip install -r requirements.txt
```

**Python 版本**：3.9+（已测试 3.9.23）

**特殊依赖**：TA-Lib 需预装 C 库

### 2. 数据源配置

编辑 `configs/ml_baseline.yml`：

```yaml
data:
  influxdb:
    enabled: true
    url: "http://localhost:8086"
    org: "stock"
    bucket: "stock_kdata"
    token: "YOUR_TOKEN"
    
  mysql:
    host: "localhost"
    port: 3306
    user: "root"
    password: "YOUR_PASSWORD"
    database: "stock_meta"
```

### 3. 运行完整流程

```bash
# 统一入口
python main.py

# 快速模式（跳过漂移检测）
python main.py --skip_drift

# 三条线对比
python main.py --compare_all

# 仅运行特定步骤
python main.py --only prepare_data
python main.py --only prepare_factors
python main.py --only train
```

### 4. 直接运行子管道

```bash
# 数据准备 + 快照
python pipelines/prepare_data_with_snapshot.py

# 因子工程 + IC 评估
python pipelines/prepare_factors.py

# 训练 + 评估 + 回测
python pipelines/run_baseline_pipeline.py --compare_all --skip_drift
```

---

## 📊 输出产物

### 目录结构

```
ML output/
├── datasets/           # 数据快照
│   └── ds_2025Q4_v1/
│       ├── features.parquet
│       ├── labels.parquet
│       └── metadata.json
├── models/             # 模型文件
│   └── baseline_v1/
│       ├── regression_model.pkl
│       ├── regression_rank_model.pkl
│       └── lambdarank_model.pkl
├── predictions/        # 预测结果
│   └── baseline_v1/
│       └── test_predictions.parquet
├── reports/            # 评估报告
│   └── baseline_v1/
│       ├── model_comparison.json
│       ├── drift_report.json
│       ├── factor_ic_report.json
│       └── backtest_stats.json
└── figures/            # 可视化图表
    └── baseline_v1/
        ├── net_value.png
        ├── ic_timeseries.png
        ├── bucket_returns.png
        └── drawdown.png
```

### 评估报告示例

**三条线对比**：

```
任务类型                Mean IC     ICIR      ICIR(年化)   Spread
─────────────────────────────────────────────────────────────────
regression            0.0674      0.3878     6.16        0.0046
regression_rank       0.0543      0.3772     5.99        0.0017
lambdarank            0.0316      0.2101     3.34        0.0035
```

**回测绩效**：

```
策略              年化收益   波动率    夏普比率   最大回撤   Alpha
──────────────────────────────────────────────────────────────────
Top-20 等权       12.5%     18.3%     0.68      -22.1%    8.2%
基准（沪深300）    5.0%     20.5%     0.24      -35.2%    -
```

---

## ⚙️ 配置文件说明

`configs/ml_baseline.yml` 主要配置段：

| 配置段 | 说明 |
|--------|------|
| `data` | 股票池、日期范围、数据源连接、交易过滤参数 |
| `features` | 预处理参数、因子工厂配置、质量检查阈值 |
| `target` | 目标变量名、预测周期、收益类型 |
| `execution` | 执行模式（open_to_open）、执行延迟 |
| `split` | 训练/验证/测试比例、Purge/Embargo 天数 |
| `ranking` | 任务类型、LambdaRank 分桶数、NDCG 评估位置 |
| `models` | 各模型参数（Ridge/RF/LightGBM/LGBMRanker） |
| `evaluation` | 分桶数、评估指标列表 |
| `backtest` | 策略类型、调仓周期、交易成本 |

---

## 📈 因子质量标准

### 入库标准（两档）

| 标准 | IC 均值 | ICIR | p-value | 适用场景 |
|------|---------|------|---------|----------|
| 严格 | ≥ 0.02 | ≥ 0.5 | < 0.05 | 生产级因子 |
| 探索 | ≥ 0.005 | ≥ 0.15 | < 0.10 | 研究级因子 |

### 漂移监控标准

| 指标 | 阈值 | 说明 |
|------|------|------|
| 特征 PSI | < 0.2 | 分布稳定 |
| Valid vs Test IC 差 | < 30% | 预测一致 |
| 合格因子数 | ≥ 10 | 最小数量 |

---

## 🐛 常见问题

### Q1: "无法加载价格数据"

**原因**：InfluxDB 连接失败

**解决**：
1. 检查 InfluxDB 服务：`http://localhost:8086`
2. 验证 token 和 bucket 配置
3. 确认数据已导入

### Q2: IC 与回测收益不成正比

**原因**：IC 衡量预测能力，回测受交易成本、流动性、极端行情影响

**建议**：
- 控制换手率（降低调仓频率）
- 增加选股数量（分散风险）
- 行业中性化（避免风格暴露）

### Q3: Alpha 为负

**可能原因**：
1. IC 过低（< 0.02）
2. 调仓过频（成本吞噬收益）
3. 因子失效期（市场风格切换）

### Q4: LambdaRank 效果不如回归

**原因**：LambdaRank 只关心排序，对极端值不敏感

**适用场景**：
- 选股数量较少（Top-10/20）
- 只关心头部排序质量
- 原始收益分布极端

---

**开始你的量化研究之旅！** 🚀
